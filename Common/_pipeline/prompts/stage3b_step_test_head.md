Your task: write one TEST-WRITING step for aider (an AI coding tool), covering
the test files listed below. The output is a markdown block containing (a) an
aider shell command and (b) a SELF-CONTAINED implementation prompt for a local
LLM that has no access to the architecture plan.

THIS STEP GENERATES TESTS, NOT PRODUCTION CODE. The production modules under
test already exist (they were generated by earlier steps). The prompt you write
MUST instruct the LLM to:
  - IMPORT the module(s) under test from their production location.
  - Do NOT redefine, re-implement, or copy-paste the classes/functions under
    test into the test file.
  - Write test functions that assert on behavior — using pytest conventions,
    fixtures (tmp_path, monkeypatch), and mocks where the module hits the
    filesystem, a network, or hardware (pynvml, httpx, etc.).
  - The test file must be runnable with `pytest` out of the box; real side
    effects (disk writes, network calls, GPU access) must be mocked.

Step number: STEPNUM
Step title:  STEPTITLE
Test files:  AIDERFILES

Relevant architecture plan context (describing the modules under test) is
between the delimiters below. The module section should contain a
`### Testing strategy` subsection — if present, it lists the mocks, the
behaviors to assert, and the fixtures to use. EXTRACT that subsection
verbatim into your implementation prompt so the local LLM has concrete
per-bullet test cases to implement. Also extract class names, function
signatures, and return types from the module section (the LLM needs them
to write correct imports and assertions), but do NOT instruct the LLM to
redefine those symbols in the test file.

DESIGN DECISIONS: if the architecture context contains a `## Design Decisions`
section, include any decision whose rationale affects test behavior (e.g.
"Use /api/ps because it reports size_vram" → tests mock `/api/ps`) VERBATIM
in the implementation prompt body. This keeps test assertions aligned with
the production decisions the module under test implements.

===ARCHITECTURE_CONTEXT_START===
